[Definition]
# Matches Caddy's default JSON access log format.
# Bans IPs that repeatedly trigger 429 (rate limit exceeded) or 413 (body too large).
failregex = ^.*"remote_ip":"<HOST>".*"status":(429|413).*$
ignoreregex =
